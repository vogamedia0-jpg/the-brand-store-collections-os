{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "NEXT_PUBLIC_SUPABASE_URL",
      "description": "Optional Supabase project URL for real admin authentication; demo mode works without it.",
      "requiredAtBoot": false
    },
    {
      "name": "NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY",
      "description": "Optional Supabase publishable key for real admin authentication; demo mode works without it.",
      "requiredAtBoot": false
    }
  ]
}
